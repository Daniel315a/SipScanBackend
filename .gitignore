.env
sipscan_backend_accessKeys.csv
.claude/
__pycache__/
*.pyc
.venv
resources/debug_prompt.txt
resources/reply.txt
docs/

# Infraestructura / deploy — no se versiona
# (Terraform mantiene estado y k8s/secret.yaml lleva credenciales reales)
terraform/
scripts/

# k8s sí se versiona, salvo el secret con credenciales reales.
# Usar k8s/secret.example.yaml como plantilla.
k8s/secret.yaml


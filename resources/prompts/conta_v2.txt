You are an accounting assistant. Produce ONE valid JSON document only.

### Constraints
- Output: raw JSON only (no prose, no markdown).
- Follow the example structure and field names exactly.
- Use ISO date format (YYYY-MM-DD).
- Numeric fields must be numbers (no strings).
- Use accounts present in the provided PUC.
- If any third-party ("tercero") fields are unknown, set them to null.
- Totals must balance: sum(debito) == sum(credito).

### Inputs
[OCR_TEXT]
{{ ocr_text }}

[PUC_CSV "|" delimited]
{{ puc_csv }}

[EXAMPLE_JSON]
{{ example_json }}

### Task
From [OCR_TEXT], build a single accounting document JSON consistent with [EXAMPLE_JSON] and valid under [PUC_JSON]. Return only the JSON.

### Output
raw JSON only (no markdown, no backticks, no code fences).

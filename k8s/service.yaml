apiVersion: v1
kind: Service
metadata:
  name: sipscan-api
  namespace: sipscan

spec:
  type: LoadBalancer
  selector:
    app: sipscan-api
  ports:
    - name: http
      port: 80
      targetPort: 8000
      protocol: TCP

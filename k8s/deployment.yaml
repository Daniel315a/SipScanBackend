# Reemplaza PROJECT_ID con tu GCP Project ID
# Reemplaza IMAGE_TAG con el tag de la imagen (ej: latest, git sha)
apiVersion: apps/v1
kind: Deployment
metadata:
  name: sipscan-api
  namespace: sipscan
spec:
  replicas: 2
  selector:
    matchLabels:
      app: sipscan-api
  template:
    metadata:
      labels:
        app: sipscan-api
    spec:
      serviceAccountName: sipscan-api  # Vincula el rol IAM via IRSA
      initContainers:
        # Espera a que PostgreSQL esté listo antes de arrancar la API
        - name: wait-for-postgres
          image: busybox:1.36
          command:
            - sh
            - -c
            - |
              until nc -z postgres 5432; do
                echo "Esperando a PostgreSQL...";
                sleep 2;
              done
      containers:
        - name: api
          # Reemplaza con tu URI de Artifact Registry:
          # REGION-docker.pkg.dev/PROJECT_ID/sipscan/sipscan:IMAGE_TAG
          image: us-central1-docker.pkg.dev/sipscan-493204/sipscan/sipscan:c2eb6ef
          imagePullPolicy: Always
          ports:
            - containerPort: 8000
          envFrom:
            - configMapRef:
                name: sipscan-config
            - secretRef:
                name: sipscan-secret
          resources:
            requests:
              cpu: "250m"
              memory: "512Mi"
            limits:
              cpu: "1"
              memory: "1Gi"
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 15
            periodSeconds: 10
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 30
            periodSeconds: 20
            failureThreshold: 3
